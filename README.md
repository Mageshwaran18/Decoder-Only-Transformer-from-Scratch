# Decoder-Only-Transformer-from-Scratch
I developed a decoder-only Transformer model from scratch using Python and NumPy. The model takes two input statements and generates a predicted output sequence based on their combined context, demonstrating the core principles of attention and sequence modeling without relying on external deep learning libraries.
